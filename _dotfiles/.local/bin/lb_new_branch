#!/usr/bin/python

import argparse

parser = argparse.ArgumentParser(description='Process some integers.')
parser.add_argument('branch', metavar='B', type=str, help='an integer for the accumulator')

args = parser.parse_args()
print(args)
